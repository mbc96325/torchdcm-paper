# Small Datasets

These datasets are small enough to keep in GitHub. Each folder contains:

- `data.csv`: canonical raw/exported table;
- `metadata.json`: source, shape, upstream URL, and checksum metadata.

Use `small_datasets.csv` as the machine-readable index.
